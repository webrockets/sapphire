= component 'components/page-title', title: 'Editing Item'

= component 'components/breadcrumb' do
  li = link_to 'Items', items_path
  li = link_to @item.id, @item
  li.is-active
    a Edit

== render 'form'
